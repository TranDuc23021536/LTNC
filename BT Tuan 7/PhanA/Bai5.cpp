#include<bits/stdc++.h>
using namespace std;
char* weird_string() { 
   char c[] = "123345";
   return c; 
}
int main() {
	cout << weird_string;
}

//chương trình cảnh báo về việc trả về địa chỉ của một biến cục bộ, đây là 1 hành vi không xác định.
//output có thể khác nhau tùy vào trình biên dịch và môi trường thực thi, nhưng thường sẽ xuất hiện 1 chuỗi ngẫu nhiên và không chính xác